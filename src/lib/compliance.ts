/**
 * Compliance helpers
 * ==================
 * Retired Aug 24, 2026: the auto-appended compensated-endorsement disclosure
 * was removed by product decision. Share text is fully user-controlled; no
 * disclosure is injected at copy/post time. File kept as a tombstone so old
 * imports fail loudly at build time rather than silently.
 */
